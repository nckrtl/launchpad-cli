services:
  redis:
    image: redis:latest
    container_name: launchpad-redis
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    restart: unless-stopped
    networks:
      - launchpad

networks:
  launchpad:
    external: true
